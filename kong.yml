_format_version: "1.1"

hosts:

service:
- name: core-api
  url: http://localhost:3000
  routes:
  - name: api-index
    paths:
    - /
    strip-path : false
    hosts : 
    - api.sabza.pk
  - name: api-helloworld
    paths:
    - /hello
    strip-path : false
    hosts : 
    - api.sabza.pk