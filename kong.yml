_format_version: "1.1"

hosts:

service:
- name: core-api
  url: http://localhost:3000
  routes:
  - name: hello-aword
    paths:
    - /hello
    strip-path : false
    hosts : 
    - master.api.sabza.pk
    - api.sabza.pk
